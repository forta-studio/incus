import React from 'react';

const Hero: React.FC = () => {
  return (
    <section 
      className="hero"
      style={{
        backgroundColor: '#242a2c',
        paddingBottom: '46%'
      }}
    >
      {/* Hero content placeholder */}
    </section>
  );
};

export default Hero;